# ============================================
# Coffee Shop - Environment Configuration
# ============================================

# ============================================
# Application Settings
# ============================================
NODE_ENV=development
PORT=3000

# ============================================
# Database Configuration - MODE 1: XAMPP Local
# ============================================
# برای اتصال به XAMPP روی لوکال هاست از این تنظیمات استفاده کنید:
# - MySQL XAMPP باید فعال باشد
# - پورت پیش‌فرض: 3306
# - یوزر پیش‌فرض: root
# - پسورد پیش‌فرض: (خالی)
# ============================================
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=coffeeshop
DB_PORT=3306

# ============================================
# Database Configuration - MODE 2: Docker
# ============================================
# اگر با Docker اجرا می‌کنید، از docker-compose.yml استفاده کنید
# و نیازی به تغییر این بخش نیست
# در docker-compose.yml تنظیمات به این صورت است:
# DB_HOST=mysql
# DB_USER=coffee_user
# DB_PASS=coffee_pass
# DB_NAME=coffeeshop
# DB_PORT=3306
# ============================================

# رمز عبور root برای MySQL در Docker (فقط برای Docker)
DB_ROOT_PASSWORD=root123

# ============================================
# JWT Configuration
# ============================================
# کلید مخفی برای JWT - در محیط تولید حتماً تغییر دهید
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# مدت زمان اعتبار توکن (مثال: 24h, 7d, 30d)
JWT_EXPIRES_IN=24h

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGIN=http://localhost:3000

# ============================================
# Startup Configuration
# ============================================
# true: دیتابیس به صورت خودکار با داده‌های اولیه پر می‌شود
# false: از seeding صرف نظر می‌شود
Startup=true

# ============================================
# Docker Configuration (اختیاری)
# ============================================
PHPMYADMIN_PORT=8080